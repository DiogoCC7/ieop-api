version: '3.7'

services:
  server:
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3001:${SERVER_PORT}
    env_file:
      - .env